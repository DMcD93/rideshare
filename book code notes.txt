Current

def book(request, journey):
	p = Passanger.objects.create(journey=(Journey.objects.get(pk=journey)), front=request.user)
	p.save()
	return HttpResponseRedirect(reverse('search:search_ride'))


Shell working code

>>> p = Passanger.objects.create(journey=(Journey.objects.get(pk=4)), front=(User.objects.get(username='monica')))
>>> print p
Passangers for Journey: 3


Shell code for final implementation

p, created = Passanger.objects.get_or_create(journey=(Journey.objects.get(pk=3)))



Intended final implementation

	p, created = Passanger.objects.get_or_create(journey=(Journey.objects.get(pk=journey)))
	if created:
		p.save(commit=False)
		p.front=request.user
		p.save()
	else:
		if p.backLeft is None:
			p.save(commit=False)
			p.backLeft=request.User
			p.save()
		else:
			p.save(commit=False)
			p.backRight=request.User
			p.save()
